<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>
<import type="com.nadeem.hindustan.R"/>
        <variable
            name="navItemModel"
            type="com.nadeem.hindustan.viewmodels.NaveItemViewModel" />
    </data>

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/ll_nav_container"
        android:layout_width="match_parent"
        android:layout_height="@dimen/nav_header_height"
        android:layout_marginTop="-10sp"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:background="@drawable/side_nav_bar"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin"
        android:theme="@style/AppTheme">

        <LinearLayout
            android:id="@+id/ll_nav_header"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:clickable="true"
            android:gravity="bottom"
            android:orientation="vertical">

            <com.nadeem.hindustan.views.roundimage.RoundedImageView
                android:id="@+id/iv_nave"
                android:layout_width="@dimen/dimen_profile_icon"
                android:layout_height="@dimen/dimen_profile_icon"
                android:background="@drawable/shape_rounded_bg"
                android:backgroundTint="@color/colorProfileBG"
                android:elevation="2dp"
                app:riv_border_color="@color/colorPrimary"
                app:riv_border_width="1dp"
                app:riv_corner_radius="5dp"
                app:srcCompat="@drawable/ic_profile_icon" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/txt_nave_name"
                    style="@style/txtStyleMedium"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:textColor="@color/colorPrimary"
                    android:text="Name"
                    android:textSize="16sp" />

                <TextView
                    android:id="@+id/txt_nave_email"
                    style="@style/txtStyle"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="2dp"
                    android:text="Email"
                    android:textColor="@color/colorPrimary"
                    android:textSize="16sp" />
            </LinearLayout>
        </LinearLayout>
    </LinearLayout>
</layout>